According to LOGAPPEND.html:2, logappend requires certain arguments. When required arguments are missing, logappend should print "invalid" and return 255 (LOGAPPEND.html:38). 

This program CRASHES on this test case (throws an uncaught exception). This should scored as a vulnerability.
